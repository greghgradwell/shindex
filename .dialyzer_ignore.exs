[
  # Baseline file for existing Dialyzer warnings
  # Generated during initial setup - warnings here will be ignored
  # Remove entries as they are fixed to prevent regressions

  # To populate:
  # 1. Run: mix dialyzer 2>&1 | tee dialyzer_initial.txt
  # 2. Review warnings
  # 3. Add patterns for warnings to ignore (temporarily)

  # Format: {"lib/module.ex", :warning_type, line_number, "pattern"}
]
